# Redis Or KeyDB Plus MongoDB

Purpose: workflow state, caching, and document workflows.

Common change surfaces:

- connection config
- data access layer
- TTL or aggregation behavior
- seed/reset scripts

Testing:

- real container-backed integration tests
- isolated fixtures and databases for test
