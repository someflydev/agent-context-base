## Worker Capability

Worker processes need explicit readiness, claim, retry, and completion behavior. Long-running autonomy is acceptable only when work ownership and checkpoint semantics are visible.
