## Context Budget Discipline

Long unattended sessions degrade when the active context bundle grows without discipline. Rehydrate only the files that govern the current boundary: startup profile, one workflow, one active stack surface, one archetype, and one validation path.

Reload more context only when the current slice justifies it.
