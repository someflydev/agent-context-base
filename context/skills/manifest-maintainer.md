# Skill: Manifest Maintainer

Use when:

- adding a new stack or archetype manifest
- changing load-order rules
- updating trigger keywords or compatibility metadata

Outputs:

- manifest edits
- matching router updates if routing behavior changed
