# Canonical Observability

Use these examples for minimal, assistant-friendly observability surfaces.

## Included Patterns

- structured JSON log line with stable keys
- Prometheus-style metric exposition with a small label set
- trace span shape with explicit parent-child linkage

## Selection Rule

Choose these examples only when the task already needs observability. They are not a reason to add telemetry to an otherwise simple change.
