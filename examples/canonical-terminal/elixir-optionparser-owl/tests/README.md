# Test Layout Note

This example follows Elixir conventions and keeps executable tests under
`test/`.

The top-level `tests/` directory exists so the cross-language catalog and
prompt checks can confirm a consistent terminal-example surface across all
languages.
