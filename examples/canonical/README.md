# Canonical Examples

This folder contains preferred implementation references.

Rules:

- keep one preferred example per pattern family
- keep examples small and runnable when possible
- retire stale examples rather than letting them silently compete

Pattern families currently scaffolded:

- `prompt-first/`
- `api-endpoints/`
- `htmx-plotly/`
- `smoke-tests/`
- `docker-compose/`
