# Docker Compose Examples

Preferred examples in this family should show:

- `docker-compose.yml`
- `docker-compose.test.yml`
- repo-derived Compose `name:` values
- non-default host ports
- isolated test env files, volumes, and seed/reset flows
