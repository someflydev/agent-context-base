# Zig Zap JSON Endpoint Example

This example shows the preferred Zig plus Zap shape for a minimal JSON endpoint:

- keep the path dispatch explicit
- encode the response shape deliberately
- keep the route body small and deterministic

Use it with:

- `context/workflows/add-api-endpoint.md`
- `context/stacks/zig-zap-jetzig.md`
- `context/doctrine/testing-philosophy.md`

Related files:

- `examples/canonical-api/zig-zap-json-endpoint-example.zig`
- `examples/canonical-api/zig-zap-jetzig-example/main.zig`
