# Canonical Faker Examples

This directory holds the canonical multi-language faker arc for the TenantCore
synthetic dataset domain. The current shared contract lives in
`domain/schema.md`, and the planned language surfaces are tracked in
`CATALOG.md`.

Status: in progress. See `CATALOG.md` for per-language completion status.
