# Ruby Schema Validation Examples

- `dry-validation/` — Lane A, contract objects with schema plus semantic rule blocks
- `dry-schema/` — Lane A/B, lower-level structural schema layer with optional JSON Schema export

Ruby's examples intentionally separate structure from higher-level validation,
mirroring the dry-rb ecosystem's design.
