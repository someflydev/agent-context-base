[package]
name = "workspace-sync-rust-garde"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
garde = { version = "0.20", features = ["derive", "regex", "email"] }
