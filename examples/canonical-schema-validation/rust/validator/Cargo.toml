[package]
name = "workspace-sync-rust-validator"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
validator = { version = "0.18", features = ["derive"] }
regex = "1"
once_cell = "1"
