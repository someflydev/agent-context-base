# Elixir Schema Validation Examples

- `ecto-changeset/` — Lane A, cast-then-validate boundary pipeline
- `norm/` — Lane A, declarative spec conformance
- `ex-json-schema/` — Lane B, contract-first JSON Schema validation

Elixir's examples are deliberately split across boundary validation, declarative
specs, and schema-first contract workflows rather than forcing a hybrid model.
