# Kotlin Schema Validation Examples

- `konform/` — Lane A, Kotlin-first DSL validation with external validator objects
- `hibernate-validator/` — Lane A, Jakarta Bean Validation annotations for JVM frameworks

Kotlin is the comparison language where the repo explicitly contrasts a
Kotlin-native DSL with the standard annotation-driven JVM path.
