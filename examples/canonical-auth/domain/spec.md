# Canonical Auth Domain Spec

This file is a placeholder created by `PROMPT_134` so foundation docs can point
at a stable location without breaking manifest or navigation validation.

The full shared domain contract lands in `PROMPT_135`. That prompt will define:

- entities for users, tenants, groups, and memberships
- the permission catalog
- JWT claim shape
- route metadata registry
- `/me` response shape
- fixture corpus and parity notes
