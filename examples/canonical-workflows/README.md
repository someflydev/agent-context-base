# Canonical Workflow Artifacts

Use this category for compact artifacts that help assistants finish an implementation pass cleanly.

Preferred artifacts here should be:

- short enough to use during a real session
- tied to an existing workflow, not a new architecture layer
- explicit about validation, docs, and commit hygiene

