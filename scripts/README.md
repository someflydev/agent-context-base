# Scripts

This folder is for validation and bootstrap helpers.

Current helper:

- `validate_context_layout.py`: checks that the expected base-repo files and directories exist

Future helpers:

- manifest validation
- context bundle preview
- stale example detection
- AGENT/CLAUDE consistency checks
