# Kotlin http4k Exposed Minimal Harness

This harness probes the canonical Kotlin runtime bundle. It can run a Docker-backed smoke check everywhere and an optional native Gradle compile when host Kotlin tooling is available.
