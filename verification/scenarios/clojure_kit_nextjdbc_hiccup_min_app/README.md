# Clojure Kit next.jdbc Hiccup Minimal Harness

This harness probes the canonical Clojure runtime bundle through Docker-backed smoke checks so the example remains verifiable without host Clojure tooling.
