# Go Echo Minimal Harness

This harness probes the canonical Go Echo runtime bundle. It can run a Docker-backed smoke check everywhere and an optional native `go build` check when the Go toolchain is available.
