# Polars Data Pipeline Harness

This harness imports the canonical storage and seed-data examples with stubbed `duckdb` and `polars` modules, then checks deterministic output order and isolated seed paths.
