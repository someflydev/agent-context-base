# Rust Axum Minimal Harness

This harness probes the canonical Rust Axum runtime bundle. It can run a Docker-backed smoke check everywhere and an optional native `cargo build` check when Rust is available.
