# Nim Jester HappyX Minimal Harness

This harness probes the canonical Nim Jester plus HappyX runtime bundle. It uses Docker-backed smoke checks by default so the backend example remains verifiable without host Nim tooling.
