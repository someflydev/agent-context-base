# Scala Tapir http4s ZIO Minimal Harness

This harness probes the canonical Scala runtime bundle. It can run a Docker-backed smoke check everywhere and an optional native `sbt compile` check when Scala tooling is available.
