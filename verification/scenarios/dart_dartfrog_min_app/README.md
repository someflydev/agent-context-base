# Dart Dart Frog Minimal Harness

This harness probes the canonical Dart Frog runtime bundle. It uses Docker-backed smoke checks by default so the backend example remains verifiable without host Dart tooling.
