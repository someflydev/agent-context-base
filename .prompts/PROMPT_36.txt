You are working inside the agent-context-base repository.

This prompt is the third language-specific follow-on to `PROMPT_33`.

Your task is to add real canonical data acquisition examples, metadata, and accurate verification coverage for these stacks:

- `clojure-kit-nextjdbc-hiccup`
- `scala-tapir-http4s-zio`
- `kotlin-http4k-exposed`

This prompt should continue extending the polyglot data-acquisition layer into strongly typed and JVM-adjacent stacks that already have credible canonical API coverage in the repository.

The examples must be real code in each language, not pseudocode.

Organize the work into commit-ready batches.

For each batch:
1. Give the batch title.
2. Explain the purpose.
3. List created or updated files.
4. Provide the full contents of each file.
5. End with a suggested commit message with a bracketed prefix such as `[DATA_POLY_C_XX]`.

IMPORTANT CONSTRAINTS

- Build on the invariant layer introduced by `PROMPT_33`.
- Use the exact stack identifiers listed above.
- Keep examples small, real, and idiomatic.
- Make the invariant parallels visible without forcing identical structure.
- Verification claims must stay strictly honest.
- Reuse and extend the existing verification surfaces where practical:
  - `verification/examples/clojure/`
  - `verification/examples/scala/`
  - `verification/examples/kotlin/`
- If an example remains structure-verified only, say so explicitly in the metadata and support matrix.

SECTION 1 — Coverage overview

Briefly explain why these three stacks belong together:

- all three already have multi-file canonical API support in the repo
- all three are good fits for data normalization, persistence-bound service layers, and sync orchestration boundaries
- this batch should prove that the data-acquisition capability generalizes beyond Python and lightweight systems-language examples

SECTION 2 — Deterministic example files

Create real canonical data acquisition example files under `examples/canonical-data-acquisition/`.

Use deterministic names such as:

- `clojure-kit-source-sync-example.clj`
- `scala-tapir-source-sync-example.scala`
- `kotlin-http4k-source-sync-example.kt`

If companion Markdown explainers are helpful, use matching names:

- `clojure-kit-source-sync-example.md`
- `scala-tapir-source-sync-example.md`
- `kotlin-http4k-source-sync-example.md`

Each example should present a believable acquisition-oriented mini-surface, such as:

- a source fetch / archive function
- a normalized record type or shape
- a parser boundary
- provenance and raw-path handling
- a sync handler / service / module boundary

SECTION 3 — Language-specific expectations

For all three languages, preserve these semantics:

- raw captures happen before destructive transforms
- normalized records keep provenance
- source-specific field oddities do not become the canonical schema
- checkpoint / cursor / resume semantics stay visible
- parse and normalize logic can plausibly run from archived raw payloads

Language-specific guidance:

Clojure:
- keep the example credible for Kit plus next.jdbc plus Hiccup conventions
- favor clear boundary functions over opaque macro-heavy surfaces

Scala:
- keep the example believable for Tapir plus http4s plus ZIO
- make effect boundaries, DTOs, and sync steps explicit rather than schematic

Kotlin:
- keep the example credible for http4k plus Exposed
- make service / repository / acquisition boundaries legible and avoid pseudo-framework glue

SECTION 4 — Verification strategy by language

Handle verification carefully.

Clojure, Scala, Kotlin:
- extend the existing language-specific verification suites so the new examples are actually checked
- if you only add structural assertions, do not inflate the verification level
- if you can add optional Docker-backed or native compile-aware checks cleanly, wire them in consistently with the repo’s current style
- keep the registry targets and support-matrix notes aligned with the true coverage

SECTION 5 — Registry, catalog, and README integration

Update:

- `examples/catalog.json`
- `verification/example_registry.yaml`
- `verification/stack_support_matrix.yaml`
- `examples/canonical-data-acquisition/README.md`

Requirements:

- every new example must appear in the relevant metadata
- stack tags must be exact
- verification levels and targets must align with tests
- the README’s language matrix must stay consistent with the registry and support matrix

SECTION 6 — Verification implementation

Update the language-specific verification suites so the new examples are actually checked.

At minimum cover:

- file existence
- expected source-shape markers that prove the example is acquisition-oriented rather than a generic API stub
- README references
- runtime bundle references if the example docs point to runnable code

Prefer extending the existing per-language suites over adding detached one-off validators unless separation materially improves maintainability.

SECTION 7 — Consistency pass

After adding the examples, perform a consistency pass across the polyglot acquisition layer.

Review and update any files that now need cleanup so the repository tells one coherent story, especially:

- `examples/canonical-data-acquisition/README.md`
- `examples/README.md`
- `verification/README.md`
- `verification/stack_support_matrix.yaml`

SECTION 8 — Commit-ready batching

Recommended batching:

Batch 1
- Clojure example plus metadata

Batch 2
- Scala and Kotlin examples plus metadata

Batch 3
- verification updates and cross-language consistency cleanup

You may adjust if needed.

SECTION 9 — Honesty rules

If a language ends this prompt with weaker verification than another, say that plainly.

Examples:

- “real Clojure example, structure-verified only”
- “real Scala example, Docker-backed runtime not added for this surface”
- “real Kotlin example, registry confidence remains medium”

The purpose is trust, not symmetry.

SECTION 10 — Output

Generate the actual implementation pass in commit-ready batches now.
